{
  "name": "ai-maestro-buddy",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered real-time music teacher for kids",
  "workspaces": [
    "client",
    "server",
    "mcp-gateway"
  ],
  "scripts": {
    "dev": "concurrently -k -p \"[{name}]\" -n \"client,server,mcp\" -c \"cyan.bold,green.bold,yellow.bold\" \"pnpm --filter client dev\" \"pnpm --filter server dev\" \"pnpm --filter mcp-gateway dev\"",
    "install-all": "pnpm install",
    "build": "pnpm -r build"
  },
  "keywords": [
    "ai",
    "music",
    "education",
    "rhythm",
    "gemini",
    "vertex-ai"
  ],
  "author": "Jayvic San Antonio",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
